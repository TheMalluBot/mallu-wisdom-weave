
// This component is no longer needed with the clean design approach
// The Hero section now uses simple gradients and minimal backgrounds
const HeroBackground = () => {
  return null;
};

export default HeroBackground;
